package com.ailegal.advisor;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Verifies the Spring application context loads correctly.
 * Acts as a smoke test — if beans are misconfigured this will fail.
 */
@SpringBootTest
class AiLegalAdvisorApplicationTests {

    @Test
    void contextLoads() {
        // If this passes, all beans wired correctly
    }
}
